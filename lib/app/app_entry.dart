import '../features/chat_first_prototype/chat_first_app.dart';
import '../features/chat_first_prototype/chat_first_controller.dart';
import 'package:flutter/widgets.dart';

Widget buildIterApp({ChatFirstPrototypeController? controller}) {
  return ChatFirstPrototypeApp(controller: controller);
}
